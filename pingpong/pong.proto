syntax = "proto3";
import "google/protobuf/empty.proto";
package chat;

option go_package = "./";

service PongService {
    rpc SayHello(google.protobuf.Empty) returns(Pong) {}
}

message Ping {
    string ping = 1;
}

message Pong {
    string pong = 1;
}
